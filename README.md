# file-hide
Dependencies
1.my sql connector maven
<dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.2.0</version>
        </dependency>
2.javaX.mail dependency
<dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>javax.mail</artifactId>
            <version>1.6.2</version>
        </dependency>
    </dependencies>
    how to setup
    1.create a database in mySql with name filehider.
    2.create two tables data and users.
    users table with id,name,email;
    data table with name,path,email,bin_data.
    To run 
    1.open the welcome file and run the java file
    
